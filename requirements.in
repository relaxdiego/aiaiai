# Direct dependencies. Edit this file, then regenerate requirements.txt:
#   uv pip compile requirements.in --generate-hashes --exclude-newer 2026-09-11T00:00:00Z -o requirements.txt
#
# --exclude-newer freezes the resolver at the 1.100.1 release, so no
# dependency uploaded after it can enter the lockfile.
#
# Pinned to 1.100.1, the newest stable line. It is past every published
# LiteLLM advisory, including GHSA-3cv6-jpf6-8222 (CVE-2026-84377), which
# affects < 1.94.0 with no backport. The supply-chain-compromised 1.82.7
# and 1.82.8 releases are far behind it.
litellm[proxy]==1.100.1
prisma==0.15.0
